### VSCode extension used for Live Sass Compilation

ritwickdey.live-sass
ext install live-sass

#### VSCode Configuration

```
 "liveSassCompile.settings.formats": [
    {
      "format": "expanded",
      "extensionName": ".css",
      "savePath": "/css"
    }
  ]
```
